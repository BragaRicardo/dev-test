version: '3.8'
services:
  api:
    build:
      context: ./api
    environment:
      - ConnectionStrings__DefaultConnection=Host=db.fstiushspvzplpwuhplq.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=${DB_PASSWORD};
    ports:
      - "5000:80"